(()=>{const token=()=>localStorage.getItem('locly_token');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){const host=location.hostname||'localhost';try{const r=await fetch(`http://${host}:3002/api/security/overview`,{headers:{Authorization:'Bearer '+token()}});const d=await r.json();if(!r.ok)throw new Error(d.error||'Accès refusé');let box=document.querySelector('#locly-security-center');if(!box){box=document.createElement('section');box.id='locly-security-center';box.style='margin:24px;padding:24px;border:1px solid #d0d7de;border-radius:20px;background:#fff;font-family:system-ui,sans-serif';(document.body.querySelector('main')||document.body).appendChild(box)}const shared=d.sharedIpAlerts||[],changes=d.userIpChanges||[],events=d.recentEvents||[];box.innerHTML=`<h2 style="margin:0 0 6px">🛡️ Centre de sécurité</h2><p style="margin:0 0 18px;color:#5A7182">Surveillance des connexions et comportements inhabituels.</p><div style="display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px"><div><b>🟢 ${d.online}</b><small> en ligne</small></div><div><b>👤 ${d.accounts}</b><small> comptes actifs</small></div><div><b>⚠️ ${shared.length}</b><small> IP partagées</small></div><div><b>🚫 ${d.blockedIps}</b><small> IP bloquées</small></div></div><hr style="border:0;border-top:1px solid #eee;margin:22px 0"><h3>Alertes IP partagées</h3>${shared.length?`<ul>${shared.map(x=>`<li><b>${esc(x.ip)}</b> — ${x.accounts} comptes, ${x.sessions} sessions</li>`).join('')}</ul>`:'<p style="color:#5A7182">Aucune anomalie détectée.</p>'}<h3>Changements d'IP</h3>${changes.length?`<ul>${changes.map(x=>`<li>Utilisateur #${x.user_id} — ${x.ips} IP sur 24h : ${esc(x.addresses)}</li>`).join('')}</ul>`:'<p style="color:#5A7182">Aucun changement inhabituel détecté.</p>'}<h3>Événements récents</h3>${events.length?`<div style="overflow:auto"><table style="width:100%;border-collapse:collapse"><thead><tr><th align="left">Date</th><th align="left">Sévérité</th><th align="left">Type</th><th align="left">IP</th><th align="left">Utilisateur</th></tr></thead><tbody>${events.map(e=>`<tr><td>${esc(e.created_at)}</td><td>${esc(e.severity)}</td><td>${esc(e.type)}</td><td>${esc(e.ip||'—')}</td><td>${esc(e.email||'Visiteur')}</td></tr>`).join('')}</tbody></table></div>`:'<p style="color:#5A7182">Aucun événement.</p>'}`; }catch(e){console.warn('[Locly Security]',e.message)}}load();setInterval(load,15000)})();